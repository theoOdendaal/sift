[package]
name = "sift"
version = "0.1.0"
edition = "2024"

[dependencies]
ureq = "3.4.0"

[profile.release]
debug = true

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
criterion = "0.5"

[[bench]]
name = "xml_tokenizer"
harness = false
